# FAQ



<table><thead><tr><th width="210.5999755859375" valign="top">Error</th><th valign="top">Cause</th><th valign="top">Fix</th></tr></thead><tbody><tr><td valign="top">ModuleNotFoundError: No module named 'cv2'</td><td valign="top">venv not activated or package not installed in venv</td><td valign="top">Check prompt shows (venv). Run pip install opencv-python inside the active venv.</td></tr><tr><td valign="top">Webcam window opens but is black</td><td valign="top">Camera locked by browser/Zoom/Teams</td><td valign="top">Close all camera-using apps. Restart Python. Try index 1 if still fails.</td></tr><tr><td valign="top">cap.isOpened() returns False</td><td valign="top">Wrong camera index or no webcam found</td><td valign="top">Try VideoCapture(1) or VideoCapture(2). On virtual machines, webcam may not be passed through.</td></tr><tr><td valign="top">Window freezes after opening</td><td valign="top">waitKey() missing or set to 0</td><td valign="top">Add cv2.waitKey(1) inside the while loop. Never use waitKey(0) in a live feed loop.</td></tr><tr><td valign="top">'python' not found in terminal</td><td valign="top">Python not in system PATH</td><td valign="top">Use 'python3' instead. On Windows, reinstall Python and tick 'Add to PATH'.</td></tr><tr><td valign="top">FPS shows 0.0 or very high number</td><td valign="top">prev_time not initialised before loop</td><td valign="top">Initialise prev_time = time.time() before the while True: line, not inside it.</td></tr><tr><td valign="top">VS Code runs wrong Python version</td><td valign="top">Interpreter set to system Python, not venv</td><td valign="top">Ctrl+Shift+P → Python: Select Interpreter → choose the venv path.</td></tr></tbody></table>

